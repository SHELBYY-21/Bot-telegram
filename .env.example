# Telegram bot token from @BotFather
TELEGRAM_BOT_TOKEN=

# Optional: restrict who can use the console (Telegram user IDs, comma/space separated)
ALLOWED_USER_IDS=

# Automatic FX rates — staff never enter Buy Rate
BUY_RATE=39.89
SELL_RATE=40.00

# SQLite ledger path
LEDGER_DB=data/ce_vault.db

# Optional: OpenAI Vision for slip OCR (falls back to caption / heuristic parser)
OPENAI_API_KEY=
OPENAI_VISION_MODEL=gpt-4o-mini

# Legacy (optional) — cursor_api.py still importable for Cloud Agents tooling
CURSOR_API_KEY=
